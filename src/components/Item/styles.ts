import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 7,
    paddingTop: 14,
  },
  description: {
    flex: 1,
    fontSize: 12,
    fontWeight: 600,
  },
});
